<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/aop
           http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- Enable AspectJ Auto-Proxying -->
    <!-- This tells Spring to automatically create proxy objects for beans -->
    <!-- that are targeted by aspects, enabling AOP functionality -->
    <aop:aspectj-autoproxy />

    <!-- Bean definition for BookRepository -->
    <bean id="bookRepository" class="com.library.repository.BookRepository" />

    <!-- Bean definition for BookService with Setter Injection -->
    <bean id="bookService" class="com.library.service.BookService">
        <property name="bookRepository" ref="bookRepository" />
    </bean>

    <!-- Register the LoggingAspect as a Spring bean -->
    <!-- The aspect must be a bean in the container for Spring AOP to use it -->
    <bean id="loggingAspect" class="com.library.aspect.LoggingAspect" />

</beans>
