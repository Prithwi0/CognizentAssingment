<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/aop
           http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--
        Enable AspectJ Auto-Proxying
        =============================
        This tag registers an AutoProxyCreator that detects @Aspect-annotated
        beans and automatically wraps targeted beans with AOP proxy objects.
        Without this tag, @Aspect, @Before, @After, @Around are ignored entirely.

        proxy-target-class="false" (default): Uses JDK Dynamic Proxy (interface-based)
        proxy-target-class="true": Uses CGLIB Proxy (class-based, no interface needed)
    -->
    <aop:aspectj-autoproxy />

    <!-- Bean definition for BookRepository -->
    <bean id="bookRepository" class="com.library.repository.BookRepository">
        <property name="repositoryName" value="LibraryBookRepository" />
    </bean>

    <!-- Bean definition for BookService with Setter Injection -->
    <bean id="bookService" class="com.library.service.BookService">
        <property name="bookRepository" ref="bookRepository" />
    </bean>

    <!--
        Register LoggingAspect as a Spring bean.
        The aspect MUST be declared as a bean for Spring AOP to detect
        its @Aspect annotation and apply the advice methods.
    -->
    <bean id="loggingAspect" class="com.library.aspect.LoggingAspect" />

</beans>
